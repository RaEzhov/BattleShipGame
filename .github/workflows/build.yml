name: CmakeBuild

on: [push]

jobs:
  build_matrix:
    strategy:
      matrix:
        build_what: [server, client]

  build:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v3

      - name: Installing libraries
        run: |
          sudo apt update
          sudo apt install -y libpqxx-dev libsfml-dev cmake ninja-build

      - name: Build ${{ matrix.build_what }}
        run: |
          cd ${{ matrix.build_what }}
          cmake -G Ninja -DCNAKE_BUILD_TYPE=Release -S ./ -B ./build
          cd build
          cmake --build ./ --target battleship_${{ matrix.build_what }} --config Release
